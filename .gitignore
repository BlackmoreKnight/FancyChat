# In-game data — generated at runtime, per-character, not source
logs/

# Backup files left by editing tools
*.bak_*
*.preMod_*
*.preStrip_*
*.preRender_*
*.preCleanNew_*
*.preOpt_*
*.temp_*

# Workflow / tooling state
.claude/

# Palette-extraction artefact (developer-only)
extracted_palette.txt

# Map downloader artefacts (generated each run; not source)
maps/_summary.txt
maps/__pycache__/

# Local-only Consolas fonts and user filter data (not part of repo source)
gdifonts/consola*.ttf
filters/

# build outputs
custom gdifonts src/Release/
custom gdifonts src/gdifonttexture/
*.bak
